class User {
  String userId;
  String email;
  String password;
  String username;
  String role;
  DateTime createdAt;

  User({this.userId, this.email, this.password, this.username, this.role, this.createdAt});

}